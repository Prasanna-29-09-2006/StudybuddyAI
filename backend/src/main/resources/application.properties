# Server Port
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/studybuddydb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Prasanna@2909
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT Security Settings
jwt.secret=9a67a86c478a2f1c8936ef19a16ff255b6efcfca0d1a49f87de8e17812b1897d
jwt.expiration=86400000

# Gemini API Configuration
# IMPORTANT: Replace YOUR_GEMINI_API_KEY_HERE with your actual Gemini API key
# You can get one from: https://aistudio.google.com/app/apikey
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent
gemini.api.key=${GEMINI_API_KEY}

# =============================================
# Email / SMTP Configuration (Gmail)
# IMPORTANT: Use a Gmail App Password (NOT your regular Gmail password)
# Steps to create App Password:
#   1. Go to https://myaccount.google.com/security
#   2. Make sure 2-Step Verification is ON
#   3. Search for "App Passwords" -> Create new -> name it "StudyBuddyAI"
#   4. Copy the 16-character password (without spaces) and paste below
# =============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# ↓ REPLACE with your actual Gmail address
spring.mail.username=prasannastudybuddyai@gmail.com
# ↓ REPLACE with your 16-character Gmail App Password (NO spaces)
spring.mail.password=oeba bttl pfuf kpzg
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
